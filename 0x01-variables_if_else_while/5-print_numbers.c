#include <stdio.h>

/**
* main - logs an alphabet in lowercase
*
* Return: returns 0 and exits the program
*/
int main(void)
{
int limit;
int i;
limit = 10;

for (i = 0; i < limit; i++)
{
printf("%d", i);
}
putchar('\n');
return (0);
}